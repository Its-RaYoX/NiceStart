<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->

    <style name="Base.Theme.NiceStart" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:windowIsTranslucent">true</item>

    </style>

    <style name="Theme.NiceStart.ActionBar" parent="Theme.Material3.DayNight" />

    <style name="Theme.NiceStart" parent="Base.Theme.NiceStart"></style>

    <style name="Theme.NiceStart.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.NiceStart.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.NiceStart.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>